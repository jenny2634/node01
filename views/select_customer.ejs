<!DOCTYPE html>
<html>
  <head>
    <title>select_member</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
      <a href="/customer/insert">고객등록</a>
      <a href="/customer/delete">고객삭제</a>
      <a href="/customer/update">고객수정</a>
  <hr />
      <table border="1">
        <thead>
            <tr>
                <th>아이디</th>
                <th>이름</th>
                <th>나이</th>
                <th>비고</th>
            </tr>
        </thead>
        <tbody>
            <% for(var i=0;i<list.length;i++){ %>
            <tr>
                <td><%= list[i].id %></td>
                <td><%= list[i].name %></td>
                <td><%= list[i].age %></td>
                <td>
                  <a href="/customer/delete?id=<%= list[i].id %>">삭제</a>
                  <a href="/customer/update?id=<%= list[i].id %>">수정</a>
                </td>
            </tr>
            <% } %>
        </tbody>
      </table>

  </body>
</html>
